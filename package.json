{
  "name": "repworkbench",
  "version": "0.1.0",
  "type": "module",
  "description": "A tool to allow representational system analysts to build RepNotation models",
  "private": true,
  "scripts": {
    "test": "./scripts/run-tests",
    "build": "./node_modules/rescript/rescript build -with-deps && mkdir -p dist && npx webpack --mode development && cp ./src/index.html ./dist/index.html",
    "clean": "./node_modules/rescript/rescript clean; rm -rf ./dist",
    "format": "./scripts/format-rescript; ./scripts/format-json",
    "watch": "./scripts/watch"
  },
  "author": "rep2rep",
  "devDependencies": {
    "rescript": "^9.1.4",
    "webpack": "^5.55.1",
    "webpack-cli": "^4.8.0"
  },
  "dependencies": {
    "@rescript/react": "^0.10.3",
    "rescript-react-digraph": "file:../rescript-react-digraph"
  }
}
